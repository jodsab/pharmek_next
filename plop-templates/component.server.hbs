import React from 'react'
import {{pascalCase name}}Client from './{{pascalCase name}}.client'

interface {{pascalCase name}}Props {
  // Define tus props aquí
}

const {{pascalCase name}} = async (props: {{pascalCase name}}Props) => {
  // Fetch de datos del servidor aquí
  // const data = await fetchData()

  return <{{pascalCase name}}Client {...props} />
}

export default {{pascalCase name}}